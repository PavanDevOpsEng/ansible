---
- name: install pakages
  hosts: localhost
  become: yes
  tasks:
  - name: packge install
    ansible.builtin.dnf:
      name: "{{ item }}"
      state: latest
    loop:
    - nginx
    - mysql
    - postfix