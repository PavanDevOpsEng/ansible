---
- name: Shell module examples
  hosts: localhost
  tasks:
  - name: date display
    ansible.builtin.shell: date
    rigster: datecmd
  - name: display the date
    ansible.builtin.debug:
      msg: "{{datecmd}}"