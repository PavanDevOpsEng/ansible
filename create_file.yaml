---
- name: create a file in taget server
  hosts: web
  become: true
  tasks: 
  - name: create a file
    ansible.builtin.file:
      path: /home/ec2-user/test/text.txt
      state: touch
  - name: list the files in the path
    ansible.builtin.debug:
      msg: "File name is {{file_name}}"
